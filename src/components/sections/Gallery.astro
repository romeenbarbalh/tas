---
interface Props {
  locale: "fr" | "en";
  t: (key: string) => string;
}
const { locale, t } = Astro.props;
import { galleryImages } from "../../utils/constants";
import GallerySection from "../ui/GallerySection.tsx";
---

<GallerySection client:load locale={locale} staticImages={galleryImages} />
